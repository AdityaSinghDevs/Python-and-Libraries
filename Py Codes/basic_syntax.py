def function(argument):
  #TUPLE ARE IMMUTABLE
  tuple=(1,2,3,4,5)
  len(tuple)
  max(tuple)
  min(tuple)
  tuple + (1)
  # tuple concatenation using +
  2 in tuple #to check presence of some element
  tuple*3
  
  
  # list indexing

  #LIST ARE MUTABLE
  list=[5,66,7,8]
  list.append(2)
  list.extend([2,3,44])
  list.remove(66)
  list.pop()
  list.insert(1,33) #first value is index, second value is VALUE 
  list.reverse()
  list.count(20)  #count repetitions of certain value
  
  # list concatenation using +
  list*3
  list.index(5) #index of value 5
  list.sort() #sort in ascending order
  list.clear() #clear all list
  len(list)
  max(list) #max value of list
  min(list) #min value of list
  2 in list #to check presence of some element
  
  # tuple indexing and slicing
  
  dict={}
  dict.keys()
  dict.values()
   
  
  
  
  